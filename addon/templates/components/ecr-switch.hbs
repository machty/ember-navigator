{{#each currentStates key="key" as |s|}}
  {{! TODO: shared helper for rendering "routeable" components? }}
  {{component s.componentName
      config=s.config
      state=s.routeState
      params=s.routeState.params
      mountedRouter=this.mountedRouter}}
{{/each}}
